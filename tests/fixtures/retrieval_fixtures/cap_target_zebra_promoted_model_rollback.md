# Promoted Model Rollback Procedure

Roll back a promoted model when monitoring shows the promoted model
performing worse than the model it replaced. Do it by restoring the
previous checkpoint and confirming the promoted model's traffic has fully
drained before declaring the rollback complete.
